import { PlannedPage } from '@/components/lp/planned-page';

/** help — owned by UI-0. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='help' screen='help' track='UI-0' />;
}
