import { PlannedPage } from '@/components/lp/planned-page';

/** S-MED-01 — owned by UI-F5. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='media' screen='S-MED-01' track='UI-F5' />;
}
