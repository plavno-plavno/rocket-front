import { PlannedPage } from '@/components/lp/planned-page';

/** S-LOC-02 — owned by UI-F1. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='locations' screen='S-LOC-02' track='UI-F1' />;
}
