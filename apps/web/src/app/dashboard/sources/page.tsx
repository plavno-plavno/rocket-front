import { PlannedPage } from '@/components/lp/planned-page';

/** S-SRC-01 — owned by UI-F1. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='sources' screen='S-SRC-01' track='UI-F1' />;
}
