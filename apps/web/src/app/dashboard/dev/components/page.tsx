import { PlannedPage } from '@/components/lp/planned-page';

/** DS — owned by UI-DS. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='_ds' screen='DS' track='UI-DS' />;
}
