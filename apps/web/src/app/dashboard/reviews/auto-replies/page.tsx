import { PlannedPage } from '@/components/lp/planned-page';

/** S-REV-04 — owned by UI-F3. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='auto-replies' screen='S-REV-04' track='UI-F3' />;
}
