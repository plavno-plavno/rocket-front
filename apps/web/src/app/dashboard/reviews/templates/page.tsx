import { PlannedPage } from '@/components/lp/planned-page';

/** S-REV-02 — owned by UI-F3. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='templates' screen='S-REV-02' track='UI-F3' />;
}
