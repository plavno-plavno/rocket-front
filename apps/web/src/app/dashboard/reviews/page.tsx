import { PlannedPage } from '@/components/lp/planned-page';

/** S-REV-01 — owned by UI-F2. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='reviews' screen='S-REV-01' track='UI-F2' />;
}
