import { PlannedPage } from '@/components/lp/planned-page';

/** S-PRD-01 — owned by UI-F5. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='products' screen='S-PRD-01' track='UI-F5' />;
}
