import { PlannedPage } from '@/components/lp/planned-page';

/** S-ANL-02 — owned by UI-F4. Replace PlannedPage with the real screen (must render a components/lp template). */
export default function Page() {
  return <PlannedPage feature='review-analytics' screen='S-ANL-02' track='UI-F4' />;
}
